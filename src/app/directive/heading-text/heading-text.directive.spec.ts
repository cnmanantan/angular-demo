import { HeadingTextDirective } from './heading-text.directive';

describe('HeadingTextDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadingTextDirective();
    expect(directive).toBeTruthy();
  });
});
